---
# this is the rendering order of tags

linux:
  label: Linux

windows:
  label: Windows

docker:
  label: Docker

dll:
  label: DLL

rsync:
  label: rsync

design_patterns:
  label: Design Patterns
